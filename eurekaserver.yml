server:
  port: 9000
eureka:
  instance:
    hostname: localhost

  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: "http://localhost:${eureka.instance.hostname}:${server.port}/eureka/"
