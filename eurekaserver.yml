server:
  port: 8083
eureka:
  instance:
    hostname: localhost

  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: "http://localhost:${eureka.instance.hostname}:${server.port}/eureka/"
